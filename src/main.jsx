import React, { useEffect, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import './styles.css'

const PROVIDERS = [
  { id: 'google', name: 'Google AI (Gemini)', hint: 'AIza...', model: 'gemini-2.0-flash' },
  { id: 'openai', name: 'OpenAI', hint: 'sk-...', model: 'gpt-4o-mini' },
  { id: 'anthropic', name: 'Anthropic', hint: 'sk-ant-...', model: 'claude-3-5-haiku-latest' },
]
const initialKeys = Object.fromEntries(PROVIDERS.map(({ id }) => [id, '']))

function App() {
  const [keys, setKeys] = useState(() => JSON.parse(localStorage.getItem('iYL.apiKeys') || 'null') || initialKeys)
  const [localUrl, setLocalUrl] = useState(() => localStorage.getItem('iYL.localUrl') || 'http://localhost:11434')
  const [localModel, setLocalModel] = useState(() => localStorage.getItem('iYL.localModel') || 'llama3.2:3b')
  const [localStatus, setLocalStatus] = useState('unknown'); const [selectedProvider, setSelectedProvider] = useState('auto')
  const [prompt, setPrompt] = useState(''); const [response, setResponse] = useState(''); const [notice, setNotice] = useState(''); const [busy, setBusy] = useState(false); const [settingsOpen, setSettingsOpen] = useState(false)
  const connectedProviders = useMemo(() => PROVIDERS.filter(({ id }) => keys[id]?.trim()), [keys])
  async function checkLocalModel() { setLocalStatus('checking'); try { const r = await fetch(`${localUrl.replace(/\/$/, '')}/api/tags`); if (!r.ok) throw new Error(); setLocalStatus('connected') } catch { setLocalStatus('offline') } }
  useEffect(() => { checkLocalModel() }, [localUrl])
  function saveSettings(event) { event.preventDefault(); localStorage.setItem('iYL.apiKeys', JSON.stringify(keys)); localStorage.setItem('iYL.localUrl', localUrl); localStorage.setItem('iYL.localModel', localModel); setSettingsOpen(false); setNotice('Connections saved. Automatic routing will use the first available provider.') }
  async function sendRequest(event) {
    event.preventDefault(); setNotice(''); setResponse(''); if (!prompt.trim()) return setNotice('Add a prompt before sending it.'); if (!connectedProviders.length) return setNotice('Add at least one provider API key in Settings.'); if (localStatus !== 'connected') return setNotice('Connect the local LLM first. Compression requires it for both request and response.')
    setBusy(true)
    try { const r = await fetch('/api/chat', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ provider: selectedProvider, apiKeys: keys, models: Object.fromEntries(PROVIDERS.map(p => [p.id, p.model])), localUrl, localModel, messages: [{ role: 'user', content: prompt }], compress: true, targetRatio: 0.55 }) }); const data = await r.json(); if (!r.ok) throw new Error(data.error || 'Request failed'); setResponse(data.response); setNotice(`Request and response compressed locally. Used ${PROVIDERS.find(p => p.id === data.provider)?.name || data.provider}.`) } catch (error) { setNotice(error.message) } finally { setBusy(false) }
  }
  const statusLabel = { connected: 'Connected', checking: 'Checking…', offline: 'Not connected', unknown: 'Not checked' }[localStatus]
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">⌁</span><span>in your <b>limits</b></span></div><nav><button className="nav-item active">⌂ <span>Overview</span></button><button className="nav-item" onClick={() => setSettingsOpen(true)}>⚙ <span>Connections</span></button></nav><div className="sidebar-footer"><div className="tiny-label">LOCAL FIRST</div><p>Requests are compressed locally before and after any available provider call.</p></div></aside><main className="main-content"><header className="topbar"><div><div className="eyebrow">WORKSPACE / OVERVIEW</div><h1>Make every token count.</h1></div><button className="settings-button" onClick={() => setSettingsOpen(true)}>⚙ Settings</button></header><section className="connection-banner"><div className={`status-dot ${localStatus}`}></div><div className="banner-copy"><strong>Local LLM {statusLabel.toLowerCase()}</strong><span>{localStatus === 'connected' ? `${localModel} is ready to compress requests and responses.` : `Connect Ollama at ${localUrl} to start.`}</span></div><button className="outline-button" onClick={checkLocalModel}>Retry</button></section>{notice && <div className="notice" role="status">{notice}</div>}<section className="stats-grid"><div className="stat-card"><span>PROVIDERS READY</span><strong>{connectedProviders.length} <small>/ {PROVIDERS.length}</small></strong><div className="progress"><i style={{ width: `${connectedProviders.length / PROVIDERS.length * 100}%` }} /></div></div><div className="stat-card"><span>ROUTING</span><strong>AUTO</strong><p>Uses any configured provider</p></div><div className="stat-card"><span>LOCAL MODEL</span><strong className="model-name">{localStatus === 'connected' ? 'Online' : 'Offline'}</strong><p>{localModel}</p></div></section><section className="workspace"><div className="section-heading"><div><div className="eyebrow">OPTIMIZER</div><h2>Send a compressed request</h2></div><label className="provider-select">Route to <select value={selectedProvider} onChange={e => setSelectedProvider(e.target.value)}><option value="auto">Automatic (any available key)</option>{PROVIDERS.map(p => <option key={p.id} value={p.id}>{p.name}</option>)}</select></label></div><form onSubmit={sendRequest}><label className="field-label" htmlFor="prompt">Your prompt</label><textarea id="prompt" value={prompt} onChange={e => setPrompt(e.target.value)} placeholder="Ask anything. Your local model will reduce it before sending…" rows="7" /><div className="composer-footer"><span>{prompt.length} characters · request + response compression enabled</span><button className="primary-button" type="submit" disabled={busy}>{busy ? 'Compressing…' : 'Send request'} <span>→</span></button></div></form>{response && <div className="result"><div className="result-heading"><span className="success-icon">✓</span><strong>Compressed response</strong><span className="muted">Returned by the selected provider</span></div><p>{response}</p></div>}</section><section className="next-steps"><h2>Get started</h2><div className="step-row"><div className="step-number">1</div><div><strong>Connect your local model</strong><p>Run Ollama with the configured model for both compression passes.</p></div><button className="text-button" onClick={() => setSettingsOpen(true)}>Configure →</button></div><div className="step-row"><div className="step-number">2</div><div><strong>Add any provider key</strong><p>Automatic routing prioritizes Gemini, then OpenAI, then Anthropic.</p></div><button className="text-button" onClick={() => setSettingsOpen(true)}>Add key →</button></div></section></main>{settingsOpen && <div className="modal-backdrop" onMouseDown={e => e.target === e.currentTarget && setSettingsOpen(false)}><form className="modal" onSubmit={saveSettings}><div className="modal-heading"><div><div className="eyebrow">SETTINGS</div><h2>Connections</h2></div><button type="button" className="close-button" onClick={() => setSettingsOpen(false)}>×</button></div><p className="modal-intro">Automatic routing uses any saved key. Gemini is tried first because it commonly has a free tier; failed providers are skipped.</p><label className="field-label" htmlFor="local-url">Local LLM URL</label><input id="local-url" value={localUrl} onChange={e => setLocalUrl(e.target.value)} placeholder="http://localhost:11434" /><label className="field-label" htmlFor="local-model">Local model name</label><input id="local-model" value={localModel} onChange={e => setLocalModel(e.target.value)} placeholder="llama3.2:3b" /><div className="key-list">{PROVIDERS.map(provider => <div className="key-field" key={provider.id}><label className="field-label" htmlFor={provider.id}>{provider.name} API key</label><input id={provider.id} type="password" autoComplete="off" value={keys[provider.id]} onChange={e => setKeys({ ...keys, [provider.id]: e.target.value })} placeholder={provider.hint} /></div>)}</div><div className="modal-actions"><button type="button" className="outline-button" onClick={() => setSettingsOpen(false)}>Cancel</button><button className="primary-button" type="submit">Save connections</button></div></form></div>}</div>
}
createRoot(document.getElementById('root')).render(<React.StrictMode><App /></React.StrictMode>)
